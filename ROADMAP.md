# 实施路线图

基线：2026-09-08。当前已完成产品规划与仓库初始化工作；以下应用里程碑均待实现，日期以实际启动日为起点。

## 1. 估算方式与总体范围

采用 AI 主导的小任务迭代：一个任务交付一个可运行、可验证的结果，维护者负责范围决策和真实体验验收。工期包含实现、运行、调试与自检，不按传统多人团队排人数；默认一个主要写入者，不假定多个 AI 可无成本并行。

前端设计、React 组件、样式与交互由 **Kimi 最新正式可用模型**负责，2026-09-08 核对基线为 Kimi K3（`kimi-code/k3`）；每项前端任务开工前重新核对模型。主开发代理负责核心、文件与安全边界、集成及独立复核，默认串行交接；具体选择与交付规则见 [AI 开发流程](docs/AI_WORKFLOW.md)。

假设维护者每天可以完成一次关键反馈；可使用 Windows 开发机和现成依赖下载；基础 JS/TS 工具链正常；Mac 测试设备在 M1/M3 可安排。AI 能压缩编码时间，无法替代操作系统验收、签名凭据、真实语料积累和用户反馈等待。

构建、测试、冒烟和文档检查均在本地执行，不使用 GitHub Actions/CI，也不把远端检查作为里程碑门槛。Windows/Mac 分别在实际目标设备上验证；没有 Mac 环境时保留待验项，不省略验收或改称通过。

| 检查点/里程碑 | 累计时间目标 | 可交付物 | 不代表 |
| --- | --- | --- | --- |
| 最小闭环，M1 内部 | 1–3 个工作日 | 自制静态 HTML 打开→选择→改字→另存→浏览器复核 | 可覆盖真实文件的可靠 MVP |
| M1 · 可行性验证 | 2–4 个工作日 | 渲染隔离、静态源码映射、字节 Patch、Mac 早期冒烟 | 任意 HTML 可编辑或双平台发布完成 |
| M2 · 可用 MVP | 5–10 个工作日 | Windows 文本校稿、目录资源、Diff、备份、冲突、恢复 | 复杂动态页面可写或正式签名安装包 |
| M3 · 双平台 Alpha | 2–4 周 | Windows x64 与 Mac arm64 的功能/打包/实机证据 | 稳定性覆盖全部 HTML |
| M4 · v1.0 稳定化 | 4–8 周 | 固定语料回归、真实用户试用、文档、恢复与发布质量 | 样式编辑、图片替换或内置 AI |
| M5 · 后续探索 | 不承诺日历 | CSS 变量、图片替换、局部 AI Patch 的独立提案 | MVP 的必需依赖 |

完整文本产品范围是 M1–M4；M5 需要独立决定范围与预算。发生源码映射或事务方案失败时，先缩减可编辑范围或替换设计，再重新估算，不按日历强行发布。

## 2. M1：证明核心，而后扩展体验

任务：HAE-001 至 HAE-006。

1. 固定运行时、工具链和模块边界，在 Windows 启动隔离预览。
2. 验证静态校稿和 JS 只读预览；阻断网络、跨根文件访问和高权限 IPC。
3. 用重复文字、混合行尾、中文/Emoji、嵌套标签、隐式节点验证 DOM 与源码映射。
4. 实现纯核心 Text Patch；由独立字节比较证明不改其他内容。
5. 串起最小闭环；先另存到新文件，未具备事务保障时不开放覆盖原文件。
6. 在 Apple Silicon Mac 做同版本启动、文件路径、核心测试与最小链路早期冒烟；拿不到设备时清楚记录 pending，不伪造通过。

**退出条件：** HAE-002 的安全负向用例、HAE-003 的映射反例、HAE-004 的字节不变量和 HAE-005 的 Windows 链路通过；Mac 早期记录完成或作为明确依赖风险登记。已知不支持内容必须被拒绝编辑。

第 2 个工作日复查源码映射。超过 2 个聚焦迭代仍不能可靠映射时，M1 缩至规范静态 HTML 的单 Text 节点元素，并更新 PRD；不得引入整页保存作为应急功能。Mac 缺少设备不阻止继续 Windows MVP，但阻止“Mac 已验证”的结论。

## 3. M2：真实可用的 Windows MVP

任务：HAE-007 至 HAE-012。

在不扩展编辑种类的前提下补齐完整使用合同：选择视觉方案、目录与资源诊断、中文输入、显式应用/取消、保存、另存、备份与崩溃恢复、历史与 Diff。每一项先完成主路径，再处理该操作的取消、失败和异步过期。

HAE-007/009 的前端设计与实现，以及 HAE-008/011 等混合任务中的界面部分，按 Kimi 模型要求执行；M1 最小壳的后续界面改动和 M3 的前端适配遵循同一规则。模型分工不改变任务依赖、视觉选稿、源码保护或验收门槛。

**退出条件：** 脱敏报告的标题、日期和三处表格文本可由维护者独立修改，独立浏览器复核；取消零写入、冲突阻断、保存失败保留草稿、重启恢复均有记录；自动用例通过；已知 P0/P1 缺陷为零。

首个可运行闭环与可靠覆盖源文件的 MVP 分开标记。界面不要求品牌最终定稿，但输入、状态和恢复行为必须完整。

## 4. M3：双平台 Alpha

任务：HAE-013 至 HAE-016。

扩充样例：离线 Tailwind/Bootstrap 页面、自制图表外围文本、SVG 只读、内联脚本、缺失资源、不同编码、恶意路径、超大文档。引入这些库的测试资源前核对许可，避免下载或提交用户数据。

Windows 10/11 x64 与 macOS 13+ arm64 分别执行同一编辑流程。补齐 DPI、IME、菜单、窗口焦点、文件权限、原生视图层级与安装器。签名、公证、本地构建环境和真实机器验证独立记录。

**退出条件：** 固定回归语料通过；无 P0/P1；支持矩阵写明每个 OS/架构的证据；包的来源、校验和、签名状态与限制明确；维护者完成真实打开→修改→保存→重启流程。缺少 Mac 实机或签名时，可以保留 Windows 预发布与源码，但双平台 Alpha 里程碑保持未完成。

## 5. M4：v1.0 稳定化

任务：HAE-017。

用固定版本记录至少 2 轮真实校稿任务，涵盖新的脱敏 HTML、保存失败、恢复和不支持内容解释。根据结果修复错误与性能，不为了扩大覆盖率改变“无法定位就只读”的边界。

**退出条件：** [发布门槛](docs/RELEASE.md)全部满足；最终支持矩阵、已知限制、数据目录说明和使用文档与程序一致；每个正式发布目标有实机和签名证据。没有 Mac 证据时只发布有证据的平台版本，不称双平台完成。

## 6. M5：后续范围

| 任务 | 提案 | 开始条件 |
| --- | --- | --- |
| HAE-018 | 受控 CSS 变量修改 | v1.0 稳定，先明确变量来源、作用域、优先级与回退 |
| HAE-019 | 本地图片替换 | 明确文件复制、资源引用和多文件事务，不破坏已有资源 |
| HAE-020 | 外部 AI 生成局部修改提案 | 不扩大执行权限；提案必须经过相同校验和人工确认 |

这三项可独立放弃或推迟。布局编辑、DOM 增删和框架源码编辑仍在范围外。

## 7. 执行顺序与变更管理

源码映射与补丁验证是关键依赖；视觉细节不应阻塞纯核心实验。Mac 从 M1 暴露问题，但系统差异只能进入平台层。任务依赖、逐条验收条件和 Issue 链接见 [任务清单](docs/BACKLOG.md)。

每完成一个 Issue，更新验证证据和剩余风险。改变安全、保存或支持边界时先补 ADR，并同步 PRD、交互文档、任务与测试计划；不能只改 README 宣布支持。

下一项实施入口是 HAE-001；该任务完成后按依赖推进 HAE-002 至 HAE-005 的最小链路。
