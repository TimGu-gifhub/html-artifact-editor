# Security Policy

## 当前状态

本仓库处于规划阶段，尚无受支持的已发布应用版本。安全设计见 [架构](ARCHITECTURE.md) 和 [测试计划](docs/TEST_PLAN.md)；这些设计尚未构成实际安全保证。

## 报告安全问题

请使用 [私密漏洞报告](https://github.com/TimGu-gifhub/html-artifact-editor/security/advisories/new) 向维护者报告，包含受影响版本/commit、最小脱敏样例、复现步骤和可能影响。不要提交真实用户内容、令牌、证书或私人路径。

如果 GitHub 私密报告入口暂不可用，可以创建不含利用细节的公开 Issue 请求私密联系渠道；在私密渠道建立前保留详细样例。维护者确认收到后会协商后续处理，当前不承诺固定响应 SLA。

## 产品安全边界

用户 HTML 被当作不可信内容。预览不能获得通用文件系统、Shell、Node 或编辑器 API；应用默认阻断网络；所有写入必须经过可信 UI 和 Main 校验。隔离和负向测试降低风险，不代表能安全执行任意恶意网页或阻止所有 Electron/Chromium 漏洞。

源文件误改、错误目标覆盖、目录越界、预览扩权、敏感备份暴露、恢复失败和静默数据丢失都按高优先级处理。不要为了演示成功禁用沙箱、关闭 TLS 校验或跳过文件冲突检查。
