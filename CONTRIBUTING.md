# 贡献指南

欢迎以小范围、可复现的改进参与 HTML Artifact Editor。当前已有工具链验证壳，产品流程仍待实现。请先看 [README](README.md)、[PRD](PRD.md)、[路线图](ROADMAP.md) 和 [任务清单](docs/BACKLOG.md)。

## 开始一个改动

1. 找到已有 Issue，核对目标、依赖和验收条件；大范围功能先讨论边界。
2. 从最新 main 创建工作分支，保留自己或他人的未提交改动。
3. 一次完成一个可审查的行为；不同问题拆开，不顺带格式化或重构整个仓库。
4. 按风险执行检查，在 PR 中写明验证结果和未测项。更改支持边界要同步规格与测试矩阵。

先按 [开发说明](docs/DEVELOPMENT.md) 安装固定依赖，在本地运行 `npm run check`、`python tools/check_docs.py`（Python 3.10+）和 `git diff --check`。本项目不使用 GitHub CI，不新增 Actions 工作流；在 PR 中记录本地命令、commit、环境与结果。启动冒烟只覆盖内置验证壳；不能把它、文档检查或纯核心测试的通过称为产品流程验收。

## 提交内容

- Bug 使用最小、脱敏、自制或有许可的样例；包含 OS、版本、步骤、期望、实际结果。
- 源码保存相关改动必须有字节范围断言和失败路径证据。
- UI 改动说明中文输入、取消、焦点、关闭与异步状态的影响；设计截图不能代替运行结果。
- 不提交个人 HTML、绝对用户目录、凭据、证书、日志内容、恢复草稿和备份。
- 第三方代码、库或测试资源需记录来源与许可证，保留所需声明。

项目原创贡献按 [MIT License](LICENSE) 提供；请确保你有权提交相关内容。没有额外 CLA，后续若调整贡献政策会先公开说明。

## AI 辅助贡献

AI 工具需遵循 [AGENTS.md](AGENTS.md) 与 [AI 开发流程](docs/AI_WORKFLOW.md)。贡献者仍需核验实际行为，不能提交未经运行的成功声明。支持范围小而确定优先于大量功能。

安全问题不要在公开 Issue 中附上可利用细节或敏感样例，参见 [安全政策](SECURITY.md)。
