# HAE-007 视觉候选交付记录

状态：**候选稿与原型检查已完成，2026-09-10 维护者已选择方案 B；实际平台交互验收仍待完成**。基线为 HAE-002 的 `db3828c`；提交位置以 Git 历史和 [Issue #7](https://github.com/TimGu-gifhub/html-artifact-editor/issues/7) 为准。此节点没有交付产品编辑或文件保存。

## 本次可观察行为

[原型入口](../design/hae-007/index.html) 提供三个同任务方向：A 预览优先、B 校稿侧栏优先、C 变更复核优先。共用一份自制报告、一套组件与文案；每个方向提供正常、只读、未保存、冲突、窄窗口状态。[组件与状态清单](../design/hae-007/README.md) 记录空间、字号、颜色、焦点和取舍。

可在内存中修改标题和三处表格文字，取消第五处输入，查看四条变更，撤销/重做并切换模式。文件原文、已应用草稿与尚未应用输入分别显示。只读状态关闭输入/保存并解释原因；模式切换保护输入，保留已应用草稿。打开、保存、另存及重新加载均明确标注为演示，不调用文件系统。

演示控制器用于直接预置状态，会重置演示输入；它不属于产品工具栏，也不是未来的文件切换流程。产品仍采用既定 React/TypeScript 技术栈，原型中的固定报告节点与内存历史不能作为真实源码映射或文件事务实现。

## Kimi 实际执行记录

模型核对日期为 2026-09-08，执行跨至 2026-09-09。同一任务保持 Kimi Code **0.41.0**、完整别名 **`kimi-code/k3`**，配置中的实际 model 为 **`k3`**；未覆盖 CLI 默认推理设置，未回退其他模型。依据为 [Kimi K3 官方说明](https://www.kimi.ai/blog/kimi-k3)、[CLI 模型配置](https://moonshotai.github.io/kimi-code/en/configuration/config-files) 和本机脱敏模型字段检查。调用输出未单独回传服务端推理级别，不能将其当作已测量值。

| 调用 | 范围与实际结果 |
| --- | --- |
| `kimi -m kimi-code/k3 -p … --output-format stream-json` | 实际生成 `index.html`、`styles.css`、`app.js`、`report.html`、`README.md`。初稿生成后，主代理终止该轮以集中交接复核问题，调用不是正常完成退出。 |
| 同一会话继续，显式保持 `-m kimi-code/k3` | 修正撤销后输入不同步、组合事件期间按钮提交、抽屉焦点、原文/草稿混淆、粘贴撤销、窄窗口与提示对比度；正常退出 0。 |
| 同一会话的限定文案修正 | 只读及交互预览空态隐藏选字邀请，保留对应原因；仅修改 `app.js`，语法检查通过，正常退出 0。 |

Kimi 负责本目录前端，主代理负责复核、[检查脚本](../../tests/design/hae-007.js)、文档集成与发布；各写入阶段串行执行。原始执行日志、截图、浏览器配置与本机路径不进入公开仓库。

初稿自测曾在同一页面顶层重复声明 `const`，造成 Electron 脚本异常并挂起；主代理终止了该测试，未计为通过。临时自测脚本已移除。最终证据来自修正后的独立浏览器检查。

## 实际检查

环境：Windows 11 x64，Edge/Chromium **152**，Playwright CLI **0.1.19**；仅使用自制报告与专用浏览器会话。检查时间：2026-09-09。

**11 组通过，0 页面运行错误。** 最终运行的 87 个资源请求均指向本机原型服务。检查前后 `index.html`、`styles.css`、`app.js`、`report.html` 的 SHA-256 相同。

| 检查组 | 已执行的断言 |
| --- | --- |
| 三方案状态 | 正常/只读/未保存/冲突提示互斥，变更计数正确，文件原文区别于草稿，宽屏次要菜单隐藏 |
| 只读空态 | 三方案在文件只读或交互预览时隐藏选字邀请，保留原因；返回静态校稿恢复邀请 |
| 未应用输入 | 失焦、同目标单击/双击保留输入，Escape 取消且不新增历史 |
| 历史 | 应用、撤销、重做同步输入；有未应用输入先保护；无变化不新增历史 |
| 校稿与抽屉 | 四次实际修改、取消第五次输入；四条变更；关闭抽屉不可聚焦，Tab 循环与三条关闭路径回焦正确 |
| 模式切换 | 继续编辑保留输入，应用后继续保留草稿；只读预览关闭编辑入口，返回静态恢复草稿 |
| 组合事件 | 合成 composition 期间按钮、目标切换与快捷键不执行；结束不重放，新的明确操作可以执行 |
| 纯文本与输入撤销 | 原生键盘输入撤销独立于应用历史；标签、ampersand、中文及 Emoji 显示为文字；恢复原文清除净变更 |
| 冲突与保存演示 | 重新加载先确认，取消保留草稿；保存演示不标记已保存；对话框 Tab 与回焦正确 |
| 最小候选窗口 | 三方案在 960×640 使用变更抽屉和折叠菜单，预览及输入保留，应用/取消区域可滚动到达 |
| 错误与资源 | 无页面运行异常，无外部资源请求 |

已检查 1440×900 的三方案画面、只读/冲突状态及三方案 960×640 画面。最初用文档 `scrollWidth` 判断窄窗口溢出，发现它包含屏外已隐藏且 `inert` 的抽屉；随后核对实际可见区域边界、关闭抽屉不可聚焦以及控件可操作性。该过程没有修改产品布局或以隐藏可用控件规避检查。

本节点另执行 `node --check`、`python tools/check_docs.py` 和 `git diff --check`。原型检查独立于 `npm run check`，不能用原型通过替代桌面应用门禁。

## 在 Windows 上复跑原型检查

需要项目既定 Node/npm、Python 和已安装的 Edge。Playwright CLI 是本次开发检查工具，不是应用运行依赖。先在仓库根目录的一个终端启动只服务自制原型的本机服务：

```powershell
python -m http.server 5177 --bind 127.0.0.1 --directory docs/design/hae-007
```

另一个终端从仓库根目录运行：

```powershell
New-Item -ItemType Directory -Path test-results/output/playwright -Force
Set-Location test-results
npx --yes --package @playwright/cli@0.1.19 playwright-cli -s=hae-design open http://127.0.0.1:5177/ --browser msedge
npx --yes --package @playwright/cli@0.1.19 playwright-cli -s=hae-design --raw run-code --filename ../tests/design/hae-007.js
npx --yes --package @playwright/cli@0.1.19 playwright-cli -s=hae-design close
```

结果对象必须为 `status: "passed"` 且 `failures` 为空，不能只根据 CLI 进程退出码认定通过。截图写入忽略目录 `test-results/output/playwright/`。检查后在服务终端 Ctrl+C 停止服务。

## 尚未完成

- 方案 B 已选定；新增实时预览、复核、窗口与 PDF 合同及产品接线见 [HAE-009](HAE-009.md)。
- 真实 Windows 输入法与系统剪贴板粘贴、Narrator/VoiceOver、系统 DPI、多显示器与 Mac 实机验收。
- 产品 UI 与不可信 WebContentsView 的原生层级、焦点和 IME 集成。
- 真实文件打开、源码定位、字节 Patch、备份、覆盖保存、冲突检测与崩溃恢复。

以上是原型阶段的证据与边界；后续核心及产品实现以对应实施记录为准。剩余人工交互检查继续保留，不由选稿或自动测试代替。
