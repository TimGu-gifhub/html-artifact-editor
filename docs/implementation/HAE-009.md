# HAE-009 · 方案 B 实时校稿工作台

日期：2026-09-10。状态：Windows 产品开发预览已接通；Issue 保持开放以跟踪真实输入法及维护者验收。基线提交为 `9f8b61f`，实际交付提交以 Git 历史和 [Issue #9](https://github.com/TimGu-gifhub/html-artifact-editor/issues/9) 为准。M2 尚未整体验收。

## 可见行为

- 正常 `npm run dev` / `npm start` 打开产品工作台。使用方案 B，在静态页面点选支持的单个 Text，可信校稿栏中输入，停顿约 250 毫秒自动预览，无需点击应用。
- 组词开始即向 Main 同步保护标志；组词结束后更新。串行 change/Apply 保留最新待发文字，旧文档/token/世代的结果不能修改新绑定。Escape 取消待预览输入，已预览内容通过还原文件原文或 Undo/Redo 恢复。
- 复核列表显示真实原文/新文，可逐条勾选或全选。再次修改某条后清除该条标记。只有全部当前净变更复核且最新源码 Diff 未过期才允许覆盖保存，保存继续先备份、原生替换和回读核验。
- 校稿栏可隐藏或拆成真实独立窗口；浮窗原生关闭表示停靠。主窗口始终拥有同一文档/历史/权限，原生关闭先等待已接受 Save，再排空当前输入窗口。失败或组合态保留输入。
- PDF 从已确认草稿生成，支持 A4/Letter、横向及背景选项。独立查看器显示这次生成的 PDF，导出使用同一份字节；旧文档/旧修订的快照有提示。导出仅创建新 PDF，不写 HTML，不支持修改 PDF 内容、分页版式或注释导出。
- 另存 HTML、恢复记录、当前文件备份及资源诊断控件连接既有 Main 接口。整份备份恢复仍须单独原生确认，不能带着未保存草稿覆盖恢复。

原型人为只开放五处演示文字；产品验证包括原型范围之外的普通正文。实际支持边界保持不变：只编辑可唯一映射且上下文受支持的静态 Text；表单/按钮、脚本生成文字、Canvas、SVG 和歧义结构仍只读。

## 模型与文件分工

2026-09-10 核对 [官方模型目录](https://www.kimi.com/code/docs/kimi-code/models.html) 与 [Kimi K3 公告](https://www.kimi.com/news/kimi-k3)。本任务稳定使用 Kimi Code CLI **0.42.0**、完整别名 **`kimi-code/k3`**、实际配置模型 **`k3`**、thinking enabled / high。

Kimi 实际执行初次 UI 实现及后续限定修正，负责 `src/ui/**` 和其新增 UI 行为测试；主代理使用 Codex / GPT-6，负责 contracts、preload、Main 产品装配、平台 PDF 新文件导出、独立测试、文档与审查。同一时间只保留一个写入者，未改动依赖或引入应用内 AI API。原始调用日志、配置、机器路径和截图不提交公开仓库。

## 本地验证

2026-09-10 在 Windows 11 x64（build 26200）执行。开发 Node.js 24.14.1 / npm 11.19.1；Electron 44.2.0、Chromium 152.0.7977.76，存储回归使用 Electron 内嵌 Node.js 24.20.0。全部在本地运行，未使用 GitHub CI。

| 检查 | 实际结果 |
| --- | --- |
| `npm run check` | 退出 0：类型、115 个源码文件边界、325/325 单元测试、33/33 UI 检查、构建、全部 Electron 实验及 62 项依赖许可证检查通过 |
| 全量中的文件事务与进程检查 | `test:storage` 的 96 项测试在 Electron 内嵌 Node 下通过；保存会话、启动、退出、恢复、Diff 和历史实验全部通过 |
| 最后界面修正后的定向回归 | 类型检查、33/33 UI 检查、产品构建、启动冒烟及 `test:product` 的 9 组真实产品流程通过 |
| PDF 独立检查 | Poppler 渲染和 pypdf 提取通过；A4 单页的中文、Emoji、表格和草稿文字正确，查看与导出字节一致 |
| 正常入口启动 | 同一产品构建从已有 Explorer 桌面会话启动，输出 product workspace ready，窗口可见且响应；使用正常 profile，通过原有目录身份校验 |
| 文档与补丁 | `python tools/check_docs.py` 和 `git diff --check` 通过 |

全量检查通过后，最后仅补充映射失效时可复制的只读输入区域及准确的页脚文案，并增加对应产品反例；因此最后一行 UI 变更使用上表的定向回归覆盖，没有将较早的全量检查描述为最后修订上的重新执行。

真实产品测试使用同一 React、生产 preload/IPC、Main 工厂、Workers、隔离 WebContentsView 和 Windows 原生 ReplaceFileW，文件和确认由 Main 测试回调选择。覆盖实际原生选字/输入、未复核 Save 拒绝、源码 Diff、Save 进行中所有取消按钮/底层输入冻结、原文件在提交前不变、精确字节保存、保存后 Undo、独立浮窗与停靠、隐藏/窄窗抽屉、备份取消和原生关闭。合成 CompositionEvent 经过真实产品控件和 Main，但不等于真实 Windows IME。

第九组通过 Main 测试代码故意改变真实 Preview 的 Text，使组词中的映射失效；断言保留输入以可聚焦的只读 textarea 显示、没有 disabled、内容准确，且 HTML 字节未变。故障注入只存在于测试 Main，不向页面增加任何能力。

PDF 查看器已实际显示中文、Emoji、表格、页面和缩略图。独立 Poppler 渲染与 pypdf 提取核对生成文件，导出内容与内存 PDF 完整字节相同；文件包含草稿，不含编辑器控件。原 HTML/CSS 在 PDF 生成、取消和导出期间保持不变。

独立审查曾复现迟到 Apply 丢新输入、组词起始未到 Main、旧文档复核响应、保存缺失报告误称未写入、Save 模态取消按钮未冻结、映射失效提示遮住保留输入，以及 PDF 查看器空白。输入/UI 问题交由 Kimi 修正并补充回归；主代理修正独立 PDF session 内置组件资源/UUID 子框架加载，普通 HTML 和编辑器导航拒绝保持原规则。

报告位于被忽略的 `test-results/`：`product.json`、`storage-runtime.json`、各既有实验 JSON、UI/全量检查日志及产品/PDF 截图。只提交自制测试源码和脱敏结论，不上传用户文件、私有存储、原生助手或生成二进制。

首次正常入口启动从 MSIX 打包宿主派生，AppData 被重定向到该宿主包的 LocalCache，目录保护按预期返回 STORAGE_LOCATION_CHANGED；此时没有打开文档或建立 workspace-records。随后通过已有 Explorer 桌面会话启动同一构建成功，未修改代码、关闭校验、更换 profile、迁移或删除证据。直接从重定向宿主启动仍不支持，开发需使用独立 Windows 终端；详见 [启动排错](../DEVELOPMENT.md#启动环境与-appdata-重定向)。

## 仍待验收与限制

- Windows 微软拼音等真实 IME、系统剪贴板、原生打开/保存/确认对话框人工操作。
- Windows 10、DPI/系统文字放大、Narrator、键盘完整路径、多显示器和安装包。
- 维护者脱敏报告完整校稿与重启恢复人工验收。后续 [HAE-012](HAE-012.md) 已自动验证真实产品正常退出/强杀后从恢复控件恢复、复核保存、独立 Edge 重开、撤销后再次保存和整份备份恢复；这仍不能代替维护者操作。
- Mac 原生保存适配与实机验证、故障处置向导、跨会话与备份清理。目录内入口切换与静态/脚本只读模式切换已由后续 [HAE-008](HAE-008.md) 完成产品接线和自动回归。
- PDF 版式取决于原 HTML 打印样式；不自动修复分页、补齐离线被阻断资源或重新排版。

合同见 [工作台](../LIVE_WORKBENCH.md)、[产品设计](../PRODUCT_DESIGN.md)、[保存与会话](../WORKSPACE_SESSION.md)。继续补齐 M2 的故障处理，并由使用者在目标机器完成手工验收；完整结果统一记入 HAE-012。

## 第二阶段 · 隐藏静态内容校稿

2026-09-11。标签页通过页面脚本切换时，静态校稿无法显示其他预置面板。本阶段增加“显示隐藏内容 / 恢复原显示”：临时展开带 `hidden` 且含受支持静态 Text 的区域，让后续正文可以选字、实时修改并复核保存。页面脚本继续禁用；保存保留原有标签切换、属性和样式字节。合同及范围见 [隐藏静态内容](../HIDDEN_CONTENT.md)。

Main 根据核验后的 SourceIndex 生成有限展示路径，插入可撤回的屏幕样式，并通过固定隔离 world 的只读检查确认实际显示和撤回。输入排空、文档/修订绑定、同步互斥、迟到结果及关闭等待均沿用工作台约束。显示失败保留草稿；原页面样式阻止展开时验证撤回，未知结果不自动重试。

2026-09-10 再次核对官方 Kimi 模型目录及 K3 公告、本机 CLI 和配置后，固定使用 Kimi Code **0.42.0**、**`kimi-code/k3` / `k3`**、thinking enabled / high。Kimi 在同一任务中实际完成初次前端、交互复核修正，以及跨日后的长文件名布局/文案修正，共三次限定调用。其写入范围为 `app.tsx`、`shell.css`、新显隐流程及 UI 测试；Codex（GPT-6）负责 core/Main/contracts、独立测试与复核，保持一个源码写入者。长文件名可缩略并以 title 查看完整名称，状态标记和右端操作不会被推到窗口外。

本阶段新增 `test:product-hidden`，已纳入 `npm run check`。实际 Windows 11 / Electron 44.2.0 产品回归通过以下 8 组：

1. 同一帧重复点击只展开一次；两个支持区域可见，表单保持只读，源属性、映射、文字历史、禁用脚本和磁盘字节不变。
2. 长文件名同时带展开/未保存标记时，960、1280、1440 三种窗口宽度的工具栏控件全部位于可见边界内，title 保留完整名称。
3. 原先隐藏的 Text 能接受 Unicode/实体实时修改；撤回/重展保留草稿和复核，未保存前 HTML 不变。
4. Chromium 打印媒体仍遵循原页面隐藏和打印覆盖规则，实际 `printToPDF` 成功，返回屏幕后仍保持展开。打印媒体观察使用测试 Main 的既有安全 debugger，不改变生产页面权限。
5. 复核 Diff 后通过 Windows 原生事务保存；完整字节与独立预期一致，新文档恢复默认显示并拒绝旧显隐请求。
6. 保存后的页面脚本在只读模式正常切换标签；返回静态校稿重新建立映射和默认显隐，不迁移动态 DOM。
7. 窄窗菜单操作后恢复同一原生 Preview；合成组词期间 Main 拒绝显隐变更。
8. 原页面 inline important 样式阻止展开时，核验精确撤回；映射/源字节不变，不能误报已展开或 unknown。

另外使用维护者提供页面的独立兼容测试副本，通过两块隐藏面板展开、原生选字、实时修改、复核保存及原脚本只读切换；完整字节比较确认只有所选 Text 改变，维护者原文件及校稿副本保持不变。用户文件、私有路径和截图不提交公开仓库。前端单独流程 23 项及全体 UI 189 项通过，四套类型检查、构建和文档链接检查通过。

分层、安全、映射、字节 Patch、产品恢复、原有工作台、目录入口切换、模式切换、启动冒烟和许可证定向检查通过。标准 `npm test` 的 353 项中 352 项通过，已有清理恢复测试达到 60 秒后取消；该项隔离复测约 23 秒通过。标准 `test:save-session` 曾达到 90 秒聚合时限；对同一构建单独给予 180 秒聚合预算后，40 组在约 88 秒内全部通过。生产超时、单项保护和仓库测试时限未调整；标准批量命令的超时仍保留记录，不能将此阶段描述为一次完整 `npm run check` 全绿。日志和原始证据仅保存在被忽略的 `test-results/`。

这些测试使用真实产品控件，但文件选择为 Main 回调，组合事件为合成事件。真实 Windows IME、原生对话框人工操作、Windows 10、DPI/读屏与维护者完整校稿验收仍待执行。仅 CSS 隐藏、关闭的 details、脚本生成的正文和任意运行中页面编辑未加入支持范围；M2 状态不因本阶段回归而改变。

## 第三阶段 · 浏览后就地校稿

2026-09-11。根据选定的交互参考，在方案 B 中加入“浏览 / 编辑文字”分段切换、精确 Text 边框和可选就地小窗。浏览时先切到后续标签，返回校稿后通过新源码的整块子树核验保留预置正文与滚动位置；动态生成或改写的正文不迁移。小窗的新输入绑定自动聚焦一次，保持实时草稿、组词保护、还原/取消、收回侧栏与复核保存。支持边界和接口见 [浏览后就地校稿](../CONTEXTUAL_EDIT.md)。

Core 只准备有限展示计划；Main 在新映射建立后、Workspace 发布前进行独立读取、屏幕样式插入及实际显隐核验，原有文件版本、离开和历史保护保持生效。展示路径及几何数据均不授予 Text 补丁权限。独立原生装饰窗只接收已核验的矩形，透明且不可聚焦、鼠标穿透，用户 Preview DOM 不增加边框节点或属性。附近小窗仍是原有可信编辑窗口，不增加第二个输入 owner。

本次重新核对 [官方模型目录](https://www.kimi.com/code/docs/en/kimi-code/models.html) 及本机 CLI/doctor/配置，固定 Kimi Code **0.42.0**、**`kimi-code/k3` / `k3`**、high / always_thinking。Kimi 实际执行两次串行调用，均退出 0：第一次交付限定 UI、装饰 Canvas、纯展示 CSS 与 18 项新增行为检查，第二次按独立复核修正 App 动态加载和就地输入聚焦，补充 5 项行为检查。Codex（GPT-6）负责 Main/core/contracts/preload、窗口集成、独立验证和文档；同一时刻只有一个源码写入者。应用仍不接入 AI 服务。

本机 Windows 11 / Electron 44.2.0 的验证包括：

| 检查 | 实际证据 |
| --- | --- |
| 单元与 UI | 全量单元 359/359 通过；最终 UI 212/212 通过。后续展示计划边界、撤回状态等小修正另以 12 项定向单元检查复核 |
| 新增 `test:product-contextual` | 8 组真实产品流程通过，已纳入本地 `npm run check` |
| 模式延续 | 浏览第二个标签后返回校稿，实际正文和位置保留、脚本停止、映射身份重建；源属性及完整字节不变。脚本改写子树时报告 partial，保留新源码静态内容 |
| 边框与小窗 | 悬停/选择边框的 Canvas 实际像素、只收数据的 preload、窗口输入归属、自动聚焦、Unicode/实体实时预览、合成组词拒绝转移、手动定位标志、滚动与 125% 页面缩放后的坐标核对 |
| 保存和打印 | 收回侧栏、全选复核、真实源码 Diff 和 Windows 替换后，全部字节符合独立 Text 替换预期；打印媒体保留原显隐，实际 PDF 生成成功，保存后重新从源显示规则开始 |
| 既有回归 | 原工作台、模式切换、隐藏内容、安全、映射、Patch 与草稿恢复回归通过；最后的界面/几何修正再次执行工作台、模式、隐藏内容、冒烟、安全和映射检查 |
| 工具链与文档 | 四套类型检查、144 文件模块边界、产品构建、文档链接和补丁空白检查通过。npm 子进程的 PATH 过长问题通过本次进程使用精简工具链 PATH 解决，未修改系统 PATH 或依赖 |

维护者提供的兼容校稿页面另在独立临时副本上完成浏览 UIKit → 返回编辑 → 精确选择代码 Text → 就地自动聚焦/修改 → 全选复核/Diff → Windows Save → 重新浏览并切换原标签。候选与保存文件逐字节比较通过，只有所选 Text 改变；下载目录的原文件和校稿副本 hash 均未变化。真实页面及截图只留在忽略的 `test-results/`，不放入公开仓库。

回归初期曾出现边框等待超时和缩放读取到旧像素。诊断确认隐藏进程启动可让 Windows 第一次显示调用后仍报告主窗口不可见，且隐藏视图的视口/动画帧会暂停。几何测试现在先实际显示并验证窗口、等待视口一致，缩放时等待新坐标的绘制结果；生产隐藏窗口/尺寸不匹配检查未放宽。几何读取通过有限短定时器合并，追踪至多两个 Text。装饰窗和原生 Preview 需分别捕获，主 WebContents 截图不代表全部原生窗口的合成效果。

上表是分层和定向执行记录，不代表一次完整 `npm run check` 全绿，也不代替人工体验。原生文件选择/确认由 Main 测试回调完成，CompositionEvent 与手动移动标志由测试触发；真实 Windows IME、OS 鼠标穿透、实际拖动、多屏 DPI、Windows 10、读屏及维护者 M2 校稿仍待验收。字号/加粗/结构/AI 编辑、CSS-only 标签、关闭的 details、框架或脚本生成内容不在本次范围内。源码原有打印规则可能只打印默认面板，本阶段未增加打印全部标签的功能。

## 第四阶段 · 原位输入与当前面板延续

2026-09-11。正常产品入口默认原位输入：浏览切换到目标正文后，点“编辑文字”，再直接点击页面文字输入；无需打开侧栏或附近小窗。工具栏在宽/窄窗口均可打开复核、逐条或全选确认，并继续源码 Diff 保存。模式切换和核验保存后的新视图保留经完整源码证明的面板及文档滚动位置；支持 hidden 属性和仅 CSS 类控制、无 ARIA 的相邻静态面板。原文件的类、显隐属性、脚本和其他字节保持不变。

原位输入通过 Main 创建的透明无框窗口覆盖已核验 Text，字体、颜色、行高和点击光标提示只承担显示作用，不能定位补丁。几何丢失时 Main 保留同一 textarea/token 的临时输入面板；组词期间不移位、不重排、不卸载，后续几何在组词结束后处理。复核、菜单、PDF、模式/载体切换和关闭均先排空实际输入 owner。侧栏与原有浮窗继续可选。完整合同见 [浏览与原位校稿](../CONTEXTUAL_EDIT.md)。

返回浏览时先显示当前面板，第一次被 Main 接受的可信输入撤回临时样式，让原页面脚本继续切换。Main 将已接受的撤回合并并加入后续显示捕获/销毁等待；忙碌期间未接受的事件不耗尽之后真实交互的机会。合成事件不能自行撤回，未知结果不能盲目重复。保存后的显示延续是可选步骤：失败报告 partial，不把已验证的文件提交变成重试保存；源版本和新映射安装检查仍强制执行。

本次重新核对官方模型目录、Kimi CLI/doctor 及可用配置，固定 Kimi Code **0.42.0**、**`kimi-code/k3` / `k3`**、high / always_thinking。两次实际串行调用均退出 0，限定写入原位 UI、字体匹配、复核入口、纯交互助手、样式和 UI 测试；第二次按主代理复核修正几何回退、字体换行、聚焦绑定及覆盖层排空。Kimi 如实留下范围外的 Preview 类型错误，主代理修正显式类型后完成整体检查。Codex（GPT-6）负责 Main/core/contracts/preload、Windows 窗口及文件事务、独立原生验证和文档。同一时间只有一个源码写入者；应用不新增 AI 或网络服务。

本机 Windows 11 / Electron 44.2.0 实际结果：

| 检查 | 证据与边界 |
| --- | --- |
| 单元和 UI | 原有 359 项全量单元通过；之后新增的 CSS 静态计划、显示元数据拒绝和提交后显示失败共 3 项检查连同相关单元共 27 项通过。最终 UI 234 项通过 |
| 原位产品 | `npm run test:product-inline` 两种原生样例各 10 组通过，共 20 组；已纳入本地 check |
| 输入与几何 | 直接 Text 点击与 UTF-16 初始光标、源字体/位置/原生尺寸、Unicode/实体、空值和多行、Esc 取消未预览输入、同一元素/token/光标保留、100%/125% 缩放、组词期间禁止覆盖层/关闭和延后几何更新 |
| 保存与显示 | 宽窗复核 Dialog/全选、源码 Diff、Windows ReplaceFileW、整文件独立字节比较；保存后当前面板仍显示；再次浏览后可信点击恢复原脚本切换，动态改写子树拒绝迁移 |
| 原有产品回归 | 工作台 9 组、模式切换 6 组、隐藏内容 8 组、附近小窗 8 组、产品目录入口 6 组通过 |
| 分层与构建 | 四套类型检查、148 个源码文件的模块边界、正常入口构建、冒烟 6 组、安全 10 组、映射 10 组通过；此前同次任务的 Patch 原生回归及全量单元文件冲突/故障恢复检查通过 |
| 打印与样例 | 屏幕样式不影响原打印显隐，实际 PDF 生成通过；维护者兼容 HTML 的独立临时副本完成 UIKit → 原位输入 → 复核保存，源/候选/保存字节核对，下载目录原文件和校稿副本 hash 保持不变 |

初次回归曾发现旧测试用合成点击切标签、依赖旧窄窗按钮样式，以及在原生视图布局/鼠标尚未稳定时等待悬停。这些检查按实际入口和可信输入更新，新的原位流程直接点击 Text，不要求先停留悬停。几何不可用时仍要求保存同一输入元素，组词、空值、位置和文字断言没有被跳过。初次全量单元中的两条旧展示预期已按新合同更新；错误显示状态不得声称 restored。原始失败与后续通过日志均留在被忽略的 test-results，不以成功重跑抹去边界。

上述结果为本机分层与定向证据，不表示一次完整 npm run check 全绿，不使用 GitHub CI。原生选择/确认由 Main 测试回调提供，CompositionEvent 是合成事件；真实 Windows IME、OS 鼠标、多屏 DPI、Windows 10、读屏和 M2 报告人工验收仍待完成。复杂背景/变换不保证原位字体布局，可使用校稿栏；任意框架/脚本生成正文仍只读。打印保留文件原规则，可能只显示默认标签。临时显示状态不进入恢复记录，重新打开/恢复不承诺继续当前标签。

## 第五阶段 · 渐显、问答与当前显示补充

2026-09-11。部分页面的正文原本就写在 HTML 中，但要经滚动渐显、展开 details 或本地提交后才能看见；此前只延续相邻面板，切换校稿后仍会失去这些显示。本阶段用有界整树源码证明延续这些静态内容的当前屏幕外观，继续直接在原位置输入，并在复核保存后的工作台保留显示。无需把“显示隐藏内容”作为必经操作。精确预算、部分结果、打印/重开边界和真正动态正文的后续任务顺序见 [当前显示补充](../CURRENT_VIEW.md)。

隔离读取只携带固定计算属性、伪元素 transform 与 details 显示状态；不修改 DOM 属性、不迁移 JavaScript 内存、表单值或运行中 Text，不增加补丁来源。离开浏览时最多等待一秒正在运行的 CSS transition，再重新核验完整源树。首次浏览不继承初始隐藏的渐显状态；临时显示可在返回浏览后由第一项被接受的真实鼠标/键盘/滚轮交互撤回。已验证 SaveCommit 不因可选显示延续失败而被误报失败。

真实提交按钮诊断另发现响应 sandbox 会在本地 `submit` 监听器收到事件之前阻止表单提交。本阶段只在交互模式下转发真实、未取消、启用的提交按钮点击为合成 `SubmitEvent`，保留原生校验和 noValidate 语义。不放开 sandbox 的表单能力、`form-action 'none'`、网络策略或页面 API；监听器尝试实际提交仍被阻断。依赖 `isTrusted` 的 submit 监听器和服务端返回不在保证范围内。

本次重新核对官方模型目录、K3 公告与本机 CLI/doctor/可用配置，固定 **Kimi Code 0.42.0 / `kimi-code/k3` / `k3` / high / always_thinking**。两次实际串行调用均退出 0：首次限定三份 UI/现有测试文件，补充显示数目、问答提示、旧文档绑定与 partial 文案；第二次只改一处 tooltip，使部分保留时也准确。两次均通过 24 项定向 UI 和 UI 类型检查。Codex（GPT-6）负责 core/Main/contracts/preload、独立验证和文档；不以 Kimi 自评代替产品验证，应用仍离线。

定向执行的本机 Windows 11 / Electron 44.2.0 证据：

- 新增 `test:product-view` 的 7 组真实产品流程通过：渐显/问答/本地提示、静态映射与原属性、原位自动聚焦/Unicode/实体、复核 Diff/精确 Windows Save、屏幕与打印规则分离、重开原交互，以及运行时改字拒绝迁移。命令已加入本地 `check`。
- 新增 3 项显示计划/返回值约束单元检查，连同相邻面板检查共 11 项通过；原位 hidden/CSS 两种样例共 20 组和原安全回归 10 组通过。
- 维护者另提供的完整页面在独立临时副本上完成滚动渐显、展开问答、真实提交按钮、本地提示 → 返回编辑 → 直接原位输入 → 全选复核/Diff → Windows Save。13 处显示差异含 1 个展开问答被核验延续；候选和保存后的完整字节均符合独立 Text 修改预期，提供的原文件 hash 未变。页面、正文、路径和截图均只留在被忽略的测试目录。

初期本地提交监听器未触发，依据真实点击和零网络实验修正为上述隔离事件转发；没有通过移除 CSP 或放开表单解决。原生点击的测试 helper 另补充父窗口聚焦和布局稳定等待，没有跳过实际点击、保存或字节断言。完整回归结果在本节后续记录。

完整 `npm run check` 首次执行通过 365 项单元、235 项 UI 以及安全/映射/Patch/草稿/会话/目录/存储/保存/启动/退出/恢复/Diff/历史和原工作台/入口/模式检查，在隐藏内容产品回归中发现批量展开 CSS 被错误捕获为交互状态。Main 已按工具归属排除开启或显示未知的批量展开，保留其保存/切换后重置合同；没有修改原测试的源字节或显隐断言。后续修复验证另记，不把首次失败记录抹去。

文件选择/确认仍由 Main 测试回调提供。真实 Windows IME、人工对话框、OS 鼠标、多屏 DPI、Windows 10、读屏和维护者 M2 校稿未由本阶段自动验收替代；脚本/框架生成的正文仍只读。当前显示不进入保存文件、PDF、检查点或重新打开的初始状态。

修正后的最终本地门槛已分段完成：四套类型检查、151 个源码文件的分层、365/365 单元和 235/235 UI 通过；隐藏内容 8 组、附近小窗 8 组、原位 hidden/CSS 两种样例共 20 组、当前显示 7 组通过。产品恢复/保存/独立 Edge/冲突验收 7 组、中断处置 21 组、完整记录清理 14 组和 62 项依赖许可证检查通过。正常入口重新构建、启动冒烟 6 组及文档链接/补丁空白检查通过。此前完整命令已通过的底层门槛和修正后的产品门槛共同构成最终证据，不表述为首次完整命令一次全绿；没有使用 GitHub Actions。

独立 Edge 检查初次返回空 stdout，导致页面断言失败。诊断与 [Playwright 上游启动参数](https://github.com/microsoft/playwright/blob/main/packages/playwright-core/src/server/chromium/chromiumSwitches.ts) 一致：Windows 兼容层重启会丢失输出句柄。仅给测试启动加入 `--edge-skip-compat-layer-relaunch` 后，原产品验收通过；浏览器 sandbox 和产品安全配置未变。维护者页面的保存副本另通过 Playwright CLI 0.1.19 驱动已安装 Edge 152，直接以 file URL 打开、点击问答、填写演示值并触发本地提示；校稿文字、渐显和原脚本交互均核对通过，保存副本与提供的原文件 hash 均未改变。CLI 使用隔离的临时配置和离线浏览器上下文，未向仓库添加 Playwright 运行时依赖或公开私人样例。
