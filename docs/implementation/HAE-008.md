# HAE-008 实施记录：目录授权与离线诊断

对应 [Issue #8](https://github.com/TimGu-gifhub/html-artifact-editor/issues/8)。当前产品已提供目录打开、入口切换和资源诊断；本记录先保留 2026-09-09 的 Main/平台阶段证据，再记录 2026-09-10 的产品接线与独立回归。原生选择器人工操作及维护者验收仍待完成，Issue 与 M2 保持开放。

## 第一阶段交付（2026-09-09）

- 原生根目录/HTML 两步选择器适配器、可取消 Main 流程；根身份保留到入口切换，禁止自动扩权或重新授权被替换的同名目录。
- 保留嵌套入口路径与 `<base>`，加载根内共享 CSS、图片、字体；交互验证入口可执行允许的本地 JS，仍只读/离线。
- 统一 Workspace 的目录打开/入口切换命令，复用输入、组合态、离开确认、视图提交和连接撤销保护。Main 另存仍仅创建入口同目录新 HTML。
- 缺失资源、CSP 阻断 CDN/API 的有界诊断，经生产 preload/IPC 更新可信状态；URL 脱敏、类型/原因去重，Preview 无此桥。具体合同见 [目录入口与资源诊断](../PROJECT_RESOURCES.md)。

没有改动 React/CSS/产品组件，本阶段由主开发代理实施 Main/平台与测试，无 Kimi 前端执行声明。产品面板后续按已定 Kimi 分工和 HAE-007 选稿条件实现。没有新增依赖、GitHub Actions 或联网兼容例外。

## 已执行的检查

实际系统 Windows 11 x64（Windows_NT 10.0.26200）；开发 Node 24.14.1 / npm 11.19.1；Electron 44.2.0 / Chromium 152.0.7977.76 / 内嵌 Node 24.20.0。输入为测试自行创建的文件，未使用用户业务资料。

[test:project](../../tests/project/main.ts) 执行八组真实 BrowserWindow/WebContentsView、生产 preload/IPC 与 Main Workspace 实验：

| 组 | 已验证结果 |
| --- | --- |
| 两步选择 | 取消根、取消入口或选择根外 HTML 均不创建 current 文档或挂载视图 |
| 相对资源 | 中文/空格/Emoji 的根及嵌套入口、BOM/CRLF、base 与共享 CSS/图片/实际字体在 Chromium 正确加载，校稿脚本不执行 |
| 诊断传输 | 缺失 CSS、CSP 阻断 CDN 与禁用脚本有类型/原因；迟到资源失败推进版本并送达 IPC；私有路径拒绝、查询/凭据/本机路径不泄漏 |
| 输入保护 | 实际点击选择并保持未应用文字；组合态不打开入口选择器，取消/越界/拒绝离开均保留原文档、视图及输入 |
| 修改与副本 | 核验选字后纯字节修改，入口同目录另存并用同一根重开，完整独立字节期望及共享 CSS 一致；旧 documentId 拒绝 |
| 只读交互 | 独立交互预览运行本地 JS=41；在线 API 被 CSP 阻断并报告 fetch 类型；页面没有 haeWorkspace/haeEditor/require |
| 连接撤销 | UI 导航时取消未返回的根选择；迟到根答复不会启动入口选择或替换当前文档，重连保留副本 |
| 根目录替换 | 已授权目录被移动并以同名新目录替换后，入口/资源拒绝；原快照保留，原 HTML、副本、另一入口、CSS、JS、图片和字体七份完整字节均不变 |

所有网络反例指向测试自建回环监听器，整个八组实验观察到 **0 次 TCP 连接**。这证明这些样例未联网，不宣称覆盖 Chromium 的所有潜在网络接口。Network 事件最初漏掉 connect-src 拒绝的 fetch；补充非 report-only Audits CSP 事件后该反例通过，未放宽网络/CSP。

另有六项新增单元检查：两步授权/相对根、外部/私有/链接拒绝、根身份变化、取消时序、诊断隐私/去重/截断、协议缺失原因。已有命令 schema、协议和 Workspace 测试同步扩展；包含资源类型误传原型属性名的反例。

| 本地门槛 | 执行结果 |
| --- | --- |
| `npm run check` | 通过：四套 strict 类型、55 源文件边界、141 单元、八目标默认构建；6 冒烟 / 10 安全 / 10 映射 / 4 Patch / 20 草稿 / 9 IPC / 7 文档 / 10 会话 / 8 目录组；62 包许可证 |
| 最后定向复测 | 完整检查后增加 Object.hasOwn 防止原型属性误作资源类型，并补充诊断 onState 主动推送断言；再次通过 typecheck、check:boundaries、141 项单元检查和八组目录实验 |
| 文档与发布 | `python tools/check_docs.py`、`git diff --check` 及实际发布 commit/远端核对见 Issue 交付评论 |

完整检查日志 `test-results/hae008-check.log`、最后单元日志 `test-results/hae008-final-unit.log` 和目录报告 `test-results/project.json` 均被忽略。目录报告含基线 `c379bbc3bac7d418c2bfb72909044c8a2ad40d2d` 加工作区差异标记、实际版本、八组断言、连接数与七份完整文件 SHA-256；本次最终源码由 Issue 的发布 commit 确定。

## 第一阶段当时的未完成项

原生对话框适配器已实现，但上述自动实验以可控制的 Main 回调选择文件，没有实际操作 OS 对话框。产品诊断面板、编辑窗口内的只读/校稿模式切换、真实 IME、Windows 10/macOS 和维护者脱敏报告未验收。正常 dev 入口仍为只读验证壳，不能从这些集成结果推定可用 MVP。

覆盖保存的备份、冲突检测、事务 journal 与重启恢复是下一项文件工作；撤销/重做、保存点、Diff 和产品接线仍待完成。已有 unknown/cleanupPending 状态继续保留证据并拒绝后续危险操作，不开放绕过恢复的覆盖命令。

## 2026-09-10 第二阶段：产品内切换入口

基线 `d1da723`。方案 B 主窗口的“更多操作”新增“切换目录内 HTML…”，宽/窄窗口均可使用；菜单以普通文本显示目录名称和当前入口相对路径。HAE-009 已接入产品目录打开与资源诊断，本阶段补齐已有 Main 入口切换的产品控件。Issue #8 保持开放，原生选择器和维护者操作仍待验收。

动作开始固定当前 documentId，先排空真正输入窗口中的最新输入，再使用最新 Workspace 修订请求 Main 选择入口；排空期间文档变化就取消。同步 BusyGuard 排除同帧重复点击和快捷键竞争，组合态、非 idle、待清理或待复核状态拒绝开启选择器。取消、越界和离开确认失败保持当前草稿；新文件只有在原有 Main 确认/副本/结束标记流程完成后才发布。命令仍只接收 documentId/stateRevision，不传文件路径或目录授权。

独立审查发现浮窗/窄窗口下原生 Preview 与 More 菜单区域重叠：直接向 editor webContents 发送鼠标事件绕过了真实窗口层级。新增几何检查后正确失败，再由 Kimi 修正菜单展开期间的 Preview 可见状态，关闭时恢复同一个原生视图；使用 Main 已有的 bounds 协调，不依赖 CSS z-index。回归核对菜单区域不重叠、非零滚动位置和原草稿保留。

### 分工与实际模型

2026-09-10 重新核对 [官方模型目录](https://www.kimi.com/code/docs/kimi-code/models.html)、[K3 公告](https://www.kimi.com/news/kimi-k3)、本机配置及 doctor。两次实际前端调用均固定 Kimi Code CLI 0.42.0，完整别名 `kimi-code/k3`、实际模型 `k3`、thinking enabled / high。Kimi 完成 `src/ui/app.tsx`、`entry-switch.ts`、`shell.css` 和 14 个 UI 行为测试；第二次限定修正仅修改 Preview 的菜单可见条件。

Codex / GPT-6 负责独立产品验证、Main 工厂的 projectChoices 测试回调转接、构建入口、文档与审查。Main 的正常原生选择器默认值不变，未新增 IPC 或依赖。同一时刻只有一个源码写入者；原始提示、模型调用日志、配置、截图和测试私有目录均不公开。

### 产品回归

新增 `npm run test:product-entry`，并加入本地 `check`。使用真实产品 React、preload/IPC、Main、Worker 和 Preview；Main 只替代原生选择器/确认决定，不直接调用核心编辑或文件切换方法。样例为自行创建的中文/Emoji 目录、两个不同子目录内同名 HTML、共享 CSS、BOM/CRLF/实体和原有脚本；完整字节与目录身份均核对。

| 组 | 实际验证 |
| --- | --- |
| 目录打开 | 无文档时切换禁用；取消根/入口及选择根外 HTML 不安装文档；正确嵌套入口加载共享 CSS，静态校稿不运行脚本 |
| 取消与拒绝 | 取消入口、根外选择、拒绝离开均保留同一文档、文字、复核、原生 Preview 和非零滚动；不再次授权根目录 |
| 组合态 | 实际控件 compositionstart 在没有新文字时到达 Main，入口操作禁用且不调用选择器；明确结束后才能新操作 |
| 浮窗 | 待投递输入在选择器启动前排空；挂起选择期间重复菜单操作和 Ctrl+O 不开启第二请求；取消后浮窗位置、文本和原文档保留 |
| 另存后继续 | 取消复制和已存在目标导致的复制失败都不切换；新副本完整核验后切换，旧视图销毁，新文档没有旧输入/历史/复核，同一根和浮窗归属保留 |
| 窄窗与键盘 | 960×640 下菜单可见且不被原生视图覆盖；Enter 激活后经明确放弃切换，源文件与共享资源完整不变 |

测试驱动最初只发送 Enter 的 down/up，真实控件收到了按键但没有字符事件/默认激活；补齐 Electron char 事件后键盘路径通过，没有为该测试问题修改产品键盘处理。

本阶段在 Windows 11 x64 build 26200 执行；开发 Node 24.14.1 / npm 11.19.1，Electron 44.2.0 / Chromium 152.0.7977.76 / 内嵌 Node 24.20.0。检查结果：`test:ui` 47/47、`test:product-entry` 6 组、既有 `test:project` 8 组、`test:product` 9 组、`test:product-acceptance` 4 组通过；类型、源码边界、文档和补丁检查通过。完整门槛的历史执行见 HAE-009；本阶段使用以上定向回归，没有宣称重新执行全量。

报告位于被忽略的 `test-results/product-entry.json`、`project.json`、`product.json`、`product-acceptance.json` 及本阶段日志/菜单截图。几何与合成输入证据不代替真实 OS 鼠标、键盘、IME 或原生对话框人工操作；M2 的维护者校稿、产品模式切换及遗留故障处理继续待完成。
