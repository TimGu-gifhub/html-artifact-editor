# HAE-005 最小校稿闭环阶段记录

日期：2026-09-10。任务：[Issue #5](https://github.com/TimGu-gifhub/html-artifact-editor/issues/5)。已发布阶段一 `30684e3`、阶段二 `2368b24`、阶段三 `cc30c08`、阶段四 `1fc54a82dbb66348162ffce88d8b8cc2f5e57611`、阶段五 `c379bbc3bac7d418c2bfb72909044c8a2ad40d2d`、阶段六 `bb913c069bec33cf56569ab8d694b59852017b94`；第七阶段以阶段六加本次差异验证，最终提交见 Issue。**任务仍进行中**，本记录覆盖后台合同与自动实验，尚无供用户操作的校稿技术 UI。

## 前五阶段行为

真实原生点击选择标题/表格 Text 后，Main 从当前草稿取得旧值，在可取消 worker 中重建和验证基线、净 Patch 与完整新候选，再请求隔离 registry 同步核对并修改 Text.data。确认成功才发布新草稿；不写原 HTML。取消另存不写文件；显式另存可独占创建同目录新 HTML，校验完成后在新会话重开。

第二阶段补充编辑目标固定与 Main 未应用输入，第三阶段接可信 renderer IPC，第四阶段验证文档替换和原生 window.close 事件保护，第五阶段组成同一 Main 窗口会话并验证视图挂载回滚及 UI 失效重连。正常 `npm run dev` 和 `npm run preview` 入口仍只读；由 `test:draft` / `test:editor` / `test:workspace` / `test:session` 自动驱动。真实控件/IME/焦点、用户对话框和产品按钮仍未接入，不能把接口或程序触发的关闭事件当作用户完整校稿或 M1/M2 验收。

## 接口与一致性

| 部件 | 合同 |
| --- | --- |
| [DraftApply](../../src/contracts/draft.ts) | selection、draftRevision、newText 三字段；不接受路径、offset、旧值或任意操作名 |
| [Main DraftSession](../../src/main/draft/session.ts) | idle/preparing/applying/saving/uncertain/closed；准备/应用/另存期间拒绝第二个命令；冻结请求，防止异步后调用者改值 |
| [后台准备](../../src/main/draft/prepare.ts) | 五秒期限、可取消、V8 堆限制；等待 worker 真正终止后返回；恢复净 Patch 必须重新通过核心验证和当前 resultHash |
| [隔离 registry](../../src/preview/node-registry.ts) | 当前 generation/revision、选中 ID、Text 对象、连接状态、旧值和伪元素约束共同验证；同步赋值，没有观察器断开窗口 |
| [Main 确认](../../src/main/preview/source-mapping.ts) | 精确 sender/session/main frame/URL、身份、requestId、旧 revision 和新 revision；两秒无确认进入未知状态 |
| [新文件平台适配器](../../src/platform/new-file.ts) | 只允许 Main 授权目录的直接 HTML 兄弟文件；拒绝跨目录、UNC、设备/ADS、隐藏名和别名；O_EXCL 永不截断已有文件 |
| [编辑目标固定](../../src/main/preview/edit-guard.ts) | beginEditing / finishEditing；token 绑定确认过的原 Text，新的原生选择只成为意图；结束时核验最新 sequence |
| [Main 输入控制器](../../src/main/draft/input.ts) | begin/change/apply/resolve/saveCopy；保存未应用文本和 composing；busy 与输入版本拒绝过期命令；输出纯数据 [InputSnapshot](../../src/contracts/input.ts) |
| [可信 UI bridge](../../src/main/editor/bridge.ts) | WebContents 局部 IPC；固定实际主框架/会话/精确 URL，随机连接与 sequence 拒绝重放，导航和 renderer 崩溃撤销 |
| [EditorAPI](../../src/contracts/editor.ts) | 七个固定方法，前后端均核验参数；输入状态与本次 copy 结果分开，不向 UI 提供路径、范围或原始 IPC；见 [接口合同](../EDITOR_BRIDGE.md) |
| [文档准备](../../src/main/workspace/document.ts) | 新预览、映射检查、草稿、输入与 writer 全部初始化后才有替换候选，失败保留旧文档 |
| [Workspace](../../src/main/workspace/controller.ts) / [原生关闭](../../src/main/workspace/window.ts) | 精确离开确认和最终版本核验，取消/失败/未知不关闭；先拦截 close，重复请求共用确认；见 [生命周期合同](../WORKSPACE_LIFECYCLE.md) |
| [统一窗口会话](../../src/main/workspace/session.ts) | Workspace、可信桥、原生视图及关闭共用 current；激活成功且最终核验通过才提交，失效 UI 重连不改变文档 |
| [WorkspaceAPI](../../src/contracts/workspace-editor.ts) | 四个固定方法，edit 显式绑定 documentId；统一窗口状态版本与本次副本结果分开，详见 [会话合同](../WORKSPACE_SESSION.md) |

赋值后只接受唯一的目标 characterData 记录，随后更新登记值；空串保留同一个 Text 对象以便续写。无变化不产生新净 Patch 或草稿 revision，但仍使旧选择 revision 过期。同节点改回基线保留原实体字节。外部即使赋相同值，仍使映射失效。

确认丢失时保留已知候选和待确认候选，拒绝编辑/另存盲目重试；关闭服务也不清除这些对象。当前仅保留内存证据，崩溃持久化与恢复界面仍待 HAE-010，不能宣称可恢复。

另存期间冻结当前候选和命令入口，取消选择器保持原草稿；成功后仍保留原入口基线和未保存变更，不把副本创建误称为原文件已保存。适配器在独占创建后保持同一句柄，写入、sync、回读 SHA-256，前后核对目录链及文件身份、大小、时间和链接数。创建之前失败返回 failed，之后任何异常返回 unknown；不自动删除部分文件、替换文件或证据。

## 执行证据

第二阶段的输入更新只改变 Main 记录，不改变 DOM/Patch/文件。composing=true 阻止应用/取消/另存，非空未应用变更阻止打开保存选择器。应用失败保留包括非法 NUL 在内的原输入供修正；应用成功更新已应用值及输入版本。继续编辑仅清除切换意图；放弃不创建 Patch；“应用并切换”先改原目标，再接受匹配的最新意图。

目标固定期间，浏览器原生选择可以发生变化，编辑器的写入目标仍保持原 Text 身份；每次应用仍校验连接状态、值、映射和 revision。两秒未收到开始/结束确认即撤销 Main 编辑权限，不猜测 renderer 状态。外部 DOM 变化后仍保存晚到的输入，禁止应用；关闭控制器不清空已有输入和候选对象。持久化、界面关闭保护和崩溃恢复仍是后续工作。

第三阶段在真实 contextBridge/IPC 上执行输入和另存：预览页无权限，同源其他窗口无 handler，子框架无公开 API 且其真实 frame 对象被 Main guard 拒绝。测试探针直送伪造路径、额外字段/参数、旧连接或重复 sequence，均拒绝。状态订阅不传 Electron event，旧状态被过滤，回调异常不影响 Main。保存结果区分本次取消与历史成功；拒绝覆盖返回失败，原文不变。主框架导航和实际 renderer 崩溃撤销接口，Main 仍保留未应用文字和候选；选择器期间撤销接口不会启动后续写入。

第四阶段让新文档在旧页旁完成准备；真实 Chromium 已打开而源解析超过深度限制时，新页被销毁，旧页/输入/候选保持。取消离开确认、确认期间的新输入、组合态和忙碌状态均不清空当前文档。原生 window.close() 的重复请求只有一次确认，取消或另存失败不销毁窗口；副本回读正确后才可关闭。创建后注入磁盘故障时，部分文件、草稿和原窗口全部保留，并拒绝继续打开其他文档，等待恢复。

第五阶段通过同一窗口的真实 preload/IPC 完成标题和重复单元格修改、另存及副本重开。新视图附着、设置尺寸、移除旧视图三处注入故障，均在提交前恢复旧页和输入；旧文档命令不会命中新文件，即使输入 revision 碰巧相同。确认期间晚到 IPC 文字使旧决定失效。实际 renderer 崩溃取消未返回的离开确认，Main 重建页面后仍是原文档/输入/候选；旧 token 和迟到放弃决定被拒绝。导航撤销未返回的另存选择器，释放 busy 状态且不写迟到路径。原生 resize 尺寸故障设置 unavailable/cleanupPending，保留文字并阻止继续应用、保存、打开和关闭。

本机 Windows 11 x64 / Windows_NT 10.0.26200；开发 Node 24.14.1 / npm 11.19.1；Electron 44.2.0 / Chromium 152.0.7977.76 / 内嵌 Node 24.20.0。全部样例自行编写，没有用户文件、网络服务依赖或 GitHub CI。

| 检查 | 结果及证明范围 |
| --- | --- |
| 135 项单元检查 | 130 项既有检查，加四项激活/回滚/连接撤销反例与一项窗口命令 schema 检查 |
| 20 组 `test:draft` | 原有 9 组选字/草稿/另存/故障，加 6 组真实选择固定/意图/失效，以及 5 组 Main 输入/组合态/取消/保存状态检查 |
| 9 组 `test:editor` | 真实可信 preload/IPC、来源/schema/重放拒绝、输入/组合态、独立字节另存、取消后历史成功、拒绝覆盖、导航与 renderer 崩溃保留 |
| 7 组 `test:workspace` | 真实文档后段解析失败、双页准备/取消、过期确认、组合态、window.close、另存/重开及实际创建后故障 |
| 10 组 `test:session` | 单窗口身份、真实 IPC 选字/另存、三处视图故障、确认竞争、旧文档请求拒绝、renderer 崩溃重连、未返回选择器撤销、来源/重放、关闭及原生 resize 故障 |
| 独立字节期望 | 标题与第二个同文单元格变化后，用独立 Buffer 期望核对完整输出；原 HTML/CSS/JS hash 不变 |
| 文件故障 | 实际已有文件/硬链接/同名并发创建、目录替换和 junction；在创建、写完、flush 后、验证后注入磁盘错误及内容改写，全部保留原文与未知结果证据 |
| 阶段一独立 Edge 152.0.4191.66 | 回环 HTTP 读取另存 HTML；标题、单元格、pre、相对 CSS 与原 JS=41 一致。脚本捕获的 HTML/CSS/JS 三次请求均本地，零页面错误；服务器另有 favicon.ico 404 |
| 完整本地回归 | `npm run check` 通过：四套 strict 类型、51 个源文件边界、135 单元、八目标构建、6 冒烟 / 10 安全 / 10 映射 / 4 Patch / 20 草稿 / 9 编辑器 IPC / 7 文档生命周期 / 9 统一会话组及 62 项许可证；随后补充第十组原生 resize 故障并再次通过 typecheck/test:session。文档/差异门槛见 Issue 发布记录 |

Electron 日志在忽略的 `test-results/draft.json`，包含 commit/工作区、实际版本、20 组步骤和四份文件 hash；InputController 另存还单独核对回读字节。单元证据见 [状态测试](../../tests/unit/draft-session.test.mjs)、[新文件测试](../../tests/unit/new-file.test.mjs)、[编辑 schema](../../tests/unit/edit-guard-contract.test.mjs) 与 [输入 schema](../../tests/unit/input-contract.test.mjs)；集成证据见 [Electron 入口](../../tests/draft/main.ts)。测试中的 CSS selector 只寻找点击坐标，生产定位仍由原生点击、Text 对象与源索引授权。

第三阶段独立 [IPC 实验](../../tests/editor/main.ts) 的结果在 `test-results/editor.json`，含九组断言及原文/副本 hash；[命令与来源单元测试](../../tests/unit/editor-contract.test.mjs) 包含版本、字段和 frame 边界。测试专用 probe 不进入默认应用构建。来源拒绝会产生预期的 “No handler registered” 日志，真实 renderer 崩溃由测试主动触发；它们不代表产品控件或原生对话框验收。

第四阶段报告在 `test-results/workspace.json`，含七组断言与四份文件 hash，入口为 [文档实验](../../tests/workspace/main.ts)；[状态单元测试](../../tests/unit/workspace.test.mjs) 补充清理失败和强制终止准备的反例。关闭事件由 Electron API 触发，选择/确认回调由测试控制，没有真实输入法或维护者点击系统窗口按钮的验收记录。

第五阶段 [会话实验](../../tests/session/main.ts) 报告为 `test-results/session.json`，含十组断言和五份文件 hash。完整回归日志 `test-results/hae005-phase5-check.log`，最后新增尺寸故障后的类型与会话复测日志 `test-results/hae005-phase5-session-final.log`；均为忽略的本地输出。共用 IPC transport 的旧单文档实验仍通过，测试探针不进入默认应用构建。

独立 Edge 使用已有 Playwright CLI 0.1.19，无新增应用依赖。[复核脚本](../../tests/draft/reopen-copy.js) 经 CLI `run-code --filename` 执行，截图在 `test-results/output/playwright/hae005-copy-edge.png`。CLI 拒绝 file: URL，故采用仅服务该自制样例目录的回环 HTTP；这不证明 Windows 原生文件打开/保存选择器或双击本地 HTML 的验收。

## 第六阶段：持久化启动与关闭排空

先前窗口实验分别构造保存、检查点和备份端口，并使用各自临时目录。本阶段新增 [Main 持久化工厂](../../src/main/workspace/persistent-session.ts)，固定 profile 下的 workspace-records 为唯一存储位置，一次装配这些既有生产服务。启动前获取 profile 锁并同步占用运行实例，重复启动与换路径复用旧锁均拒绝；旧文件、未知记录和未确认锁原样保留，不创建替代目录。合同见 [PERSISTENT_STARTUP](../PERSISTENT_STARTUP.md)。

集成时发现两个提前返回的关闭路径：统一会话第二次 dispose 没有等待第一次结果，Workspace 清理只等待 current，未加入尚在准备或保存的操作。已改为共用 disposal 并等待操作/候选清理；共用 transport 记录已接受命令，撤销权限后等待它们结算，包含 UI 重建前的旧连接。最后核验无未知草稿/副本、待审查结果、清理失败或剩余存储占用，才允许下一个运行实例。

[真实启动实验](../../tests/startup/main.ts) 与 [独立进程](../../tests/startup/child.ts) 共 11 组，全部使用生产 Workspace/IPC/存储实现及自制 HTML：

| 路径 | 观察到的结果 |
| --- | --- |
| 固定目录被文件占据、重复窗口/异步启动 | 拒绝且保留原文件；同一时刻只有一个运行实例 |
| 五处真实点击修改标题/日期/单元格 | Apply 后原 HTML/CSS 不变；完整源码 Diff 能重建独立候选，保留 BOM/CRLF、注释与非目标字节；覆盖包含中文、Emoji、实体转义 |
| 保存后新窗口启动 | 找到同一固定存储的干净历史，恢复五步 Undo，撤销后显式保存，再还原原始备份并建立干净历史 |
| 检查点写入期间销毁窗口 | 重复 dispose 等待同一结果，第二实例一直被阻止，写入完成后才释放并能发现草稿 |
| Save 准备期间销毁窗口 | 等待私有取消与准确锁释放；原 HTML 不变，草稿和取消记录保留 |
| 改名 profile 与实际第二进程 | 旧 profile 锁不能用于新目录；独立 Electron 竞争进程被 OS 锁拒绝 |
| 草稿持久化后实际进程强杀/重启 | 新进程发现同一会话，重新授权恢复，显式保存并还原备份；源与 CSS 字节匹配 |
| Windows 已替换、提交记录正写入时销毁窗口 | 等待真实提交核对，返回 rebase-required，保留旧源/候选/提交记录和实例占用 |
| 未知目录项或原生清理无法确认 | 读取返回 DRAFT_STORAGE_REVIEW_REQUIRED 或 teardown 拒绝；证据保留，不能通过新实例绕过 |

单元补充迟到准备、保存未知结果和多命令 transport 关闭反例。完整 npm run check 已通过：四套 strict 类型、97 个源文件边界、315 项单元、默认构建与既有预览/IPC/窗口回归，以及 96 项内嵌 Node 存储、40 组保存会话、11 组启动、10 组恢复、8 组源码 Diff、31 组历史和 62 项许可证。文档检查通过 44 篇 Markdown、462 个本地链接与任务 DAG，git diff --check 通过；全部本地执行。忽略的输出为 test-results/hae005-persistent-startup-check.log 和 startup.json，报告记录基线 commit/工作区、实际运行版本与三份独立 hash。

本阶段仍是 Main 装配和生命周期实现。没有改动 src/ui 或新增产品控件，空白 transport 样例不是前端设计；没有新的 Kimi 执行。正常 dev 入口、应用 quit 协调、真实 IME/对话框/退出菜单、Windows 10/macOS 与维护者真实报告验收仍待完成；不能宣称可用 MVP。

## 第七阶段：应用退出协调

仅等待 runtime.dispose 不足以保护应用退出：Electron 可能在最后窗口关闭后直接终止，app.quit 也有不同的事件路径。本阶段增加 [Main 退出协调器](../../src/main/workspace/quit.ts)，同时接住应用和窗口事件；复用同一个原生关闭决定，清理成功后才请求实际退出。存在未管理窗口时不替其关闭，重复绑定或退出不能创建并发决定。事件依据与调用边界见 [APPLICATION_QUIT](../APPLICATION_QUIT.md)。

Workspace 记录每次已接受保存的独立完成结果，Main waitForSave 只加入这一次操作，不启动新 Save；持久化窗口在成功且无清理警告后重新核验关闭。失败、未知结果、取消或重建失败会保留原窗口。批准的关闭再通过工厂的清理屏障，等待既有队列/Worker/IPC 和存储占用校验，完成后才销毁原生窗口；无法确认时保留进程和证据。离开确认仍使用已有明确另存/放弃合同，没有隐式覆盖保存。

复核补强了清理失败后的原生关闭保护：普通 guard/IPC 可能已经拆除，因此在进入清理屏障前单独安装关闭拦截器，仅在清理成功或窗口实际销毁后移除。单元和真实存储占用实验都验证了“清理失败后再点击关闭”仍保留窗口，重复请求不能绕过缓存的失败。

[退出实验父进程](../../tests/quit/main.ts) 驱动 [13 个独立 Electron 子进程](../../tests/quit/child.ts)：

| 场景 | 执行结果 |
| --- | --- |
| 原生最后窗口关闭 | 实际 will-quit 与进程正常结束发生在 runtime 清理成功之后 |
| 重复退出/关闭、取消、组合态 | 共用一个确认；取消保留未应用文字，composing 拒绝退出，之后的新明确决定才可结束 |
| 明确另存、取消选择器、再次另存 | 首次取消保留已应用草稿；副本独占写入并核验后确认退役，原 HTML/CSS 不变 |
| 原文件保存与备份恢复期间退出 | 等待当前实际事务成功、安装新基线和清理后退出，不再调用一次 Save |
| 保存前外部冲突 / 已替换但提交记录未知 | 本次退出被阻止，分别保留外部版本或已替换候选与未知 journal；未知结果的再次退出不能重复写入 |
| 检查点排空与明确放弃 | 等待实际写入和准确结束标记，确认 retired 后才退出 |
| 退役失败或额外 Main 存储占用 | 保留窗口和进程，后续请求不能绕过失败或清理屏障 |
| 强制销毁窗口时检查点尚在写入 | 应用保持存活直至排空；没有放弃授权时保留 dirty 检查点，不补写结束标记 |
| 未管理窗口 / 重复绑定 | 原窗口及额外窗口都保留，不扩大关闭权限 |
| 确认期间实际 renderer 崩溃 | 旧确认失效、输入保留；重连后的新明确关闭决定才可退出 |

父进程在真正进程退出后独立核对完整 HTML/CSS/副本字节，并检查各子场景的退出前断言已经执行。受阻场景没有 will-quit；由测试 harness 记录证据后显式结束，不能计为正常退出。类型、21 项相关单元与 13 组目标实验已通过；补强清理失败后的再次关闭保护后，重新执行了目标实验与完整本地回归。

完整 npm run check 通过：四套 strict 类型、98 个源文件边界、318 项单元、默认构建与既有预览/IPC/窗口回归，以及 96 项内嵌 Node 存储、40 组保存会话、11 组启动、13 组退出、10 组恢复、8 组源码 Diff、31 组历史和 62 项许可证。文档检查通过 45 篇 Markdown、475 个本地链接与任务 DAG，git diff --check 通过；没有使用 GitHub CI。实际环境为 Windows x64（10.0.26200）、开发 Node 24.14.1 / npm 11.19.1、Electron 44.2.0 / 内嵌 Node 24.20.0。忽略的证据为 test-results/hae005-quit-check-final.log 和 quit.json；报告记录基线 bb913c0 加本阶段未提交改动、实际进程退出类型与完整文件 hash。

本阶段未修改产品 UI 或新增菜单，没有新的 Kimi 执行。正常入口接线、真实对话框/菜单/IME、Windows 10/macOS、关机/注销/断电及维护者报告仍待验收；此交付不代表 M2 已完成。

## 仍待完成

- 把已验证的统一窗口会话接入正常应用和 Kimi 前端，提供真实对话框、视图故障提示及用户打开/取消/另存验证；产品界面仍按 HAE-007 选稿规则执行。后续 [HAE-008](HAE-008.md) 已验证 Main 目录入口与诊断接入；编辑窗口内的只读交互模式切换仍待完成。
- 备份、冲突、journal、恢复和跨保存历史已有 [HAE-010](HAE-010.md) / [HAE-011](HAE-011.md) 的后台接线和自动实验；产品保存/恢复/历史控件与真实操作验收仍待完成。新文件适配器始终不能覆盖原文。
- Windows 10/macOS、真实 IME/原生对话框/DPI/权限 ACL/磁盘满/文件占用及维护者真实脱敏报告。

适配器只保证本地新文件实验边界：不提供对其他进程改写的强 CAS，也不声称断电后目录项持久性或网络盘/云同步盘保障。身份/hash 前后检查发现不一致即保留现场；两次检查间的外部竞态仍需后续平台事务方案评估。未知结果的恢复目前没有用户流程，因此正常产品入口尚未开放此能力。
