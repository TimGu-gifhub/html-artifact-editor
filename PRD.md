# 产品需求规格

版本：产品工作台范围修订 0.2 · 日期：2026-09-10。Windows 开发预览已接通；完成范围见 [HAE-009](docs/implementation/HAE-009.md)，M2 及目标平台人工验收仍待执行。

## 1. 产品目标

HTML Artifact Editor 面向已经拥有 HTML 成品、只需要修改少量文字的人。用户无需读源码，就能把页面中的“2025 年度报告”改成“2026 年度报告”，保存后其他源文件内容保持原样。

输入为单个本地 HTML，或用户选择的目录中的 HTML 入口及其本地资源。输出为原文件的受控修订，或用户选择的另存文件。首版不需要服务器、账号、AI 服务或数据库。

### 目标用户与任务

| 用户 | 典型任务 | 成功结果 |
| --- | --- | --- |
| AI 生成内容的使用者 | 修正报告标题、日期、说明 | 点中即改，不必重新请求 AI 生成页面 |
| 业务校对人员 | 改表格文本、标签和少量金额文字 | 清楚看到改前改后，不误触图表数据或公式 |
| HTML 交付者 | 最终交付前局部修订 | 未选择的源码字节和资源文件不变 |

金额等内容按文字编辑；本工具不计算业务数据、不验证会计正确性，也不自动更新图表和合计。编辑数字附近若存在关联信息，由用户校对其含义。

### 产品原则

1. 原始文件字节是保存依据，运行时 DOM 只用于显示和提出选择。
2. 只有能够确定唯一源码范围的内容才开放编辑；不确定时解释原因并保持只读。
3. 编辑与写盘分开。取消草稿、切换选择、查看差异都不自动覆盖源文件。
4. 支持范围可以小，但要有可复现的正确性证据。
5. 本地离线是一项运行约束，外部 CDN 资源缺失必须明确显示。

## 2. 功能优先级

| ID | 需求 | 优先级 | 验收摘要 |
| --- | --- | --- | --- |
| FR-01 | 打开单个 UTF-8 HTML | M1 必须 | 中文、空格、Emoji 路径可打开，取消文件选择无副作用 |
| FR-02 | 用户选择目录和 HTML 入口 | M2 必须 | 仅加载授权根目录内的资源，切换文件先处理草稿 |
| FR-03 | Chromium 预览与只读交互 | M1 必须 | 浏览与校稿状态明确，资源加载失败可诊断 |
| FR-04 | 点击定位单个可支持 Text 节点 | M1 必须 | 重复文字不会被批量替换，拒绝歧义定位 |
| FR-05 | 实时文本草稿及中文输入 | M2 必须 | 输入停顿自动预览；校稿栏可隐藏/拆卸；IME、取消、失焦与迟到响应符合合同 |
| FR-06 | 勾选复核与源码 Diff | M2 必须 | 逐条/全选确认当前净变更；再改取消该条复核；保存绑定最新完整 Diff |
| FR-07 | 显式保存和另存为 | M1 简化，M2 完整 | 保存失败保留草稿，另存不改变原文件 |
| FR-08 | 保存前冲突检测与备份 | M2 必须 | 外部文件有变化时阻止覆盖；备份失败不开始覆盖 |
| FR-09 | 撤销、重做和保存点 | M2 必须 | 保存后撤销形成新的未保存变更，不暗中写盘 |
| FR-10 | 本地恢复草稿及崩溃恢复 | M2 必须 | 重启能找回已持久化草稿；基线变化不会自动回放 |
| FR-11 | 离线资源诊断 | M2 必须 | 列出被阻断的 URL 和资源类型，不自动下载 |
| FR-12 | Windows/macOS 键盘、菜单与打包 | M1 早期验证，M3 完整 | 共用核心，两平台分别提供实机证据 |
| FR-13 | PDF 打印预览与导出 | M2 新增 | 从已确认草稿生成 A4/Letter、横向/背景 PDF；查看与导出同一份字节，不保存 HTML |

完整操作与边缘状态见 [产品与交互设计](docs/PRODUCT_DESIGN.md)。

## 3. 支持边界

“可浏览”表示在当前网络、权限和脚本策略下尽力渲染；“保证编辑”须同时满足 [Patch 前置条件](docs/PATCH_SPEC.md)。下表是首版目标，不代表已完成验证。

| 内容 | 浏览 | 首版编辑 | 边界说明 |
| --- | --- | --- | --- |
| UTF-8 普通标题、段落、列表、表格 Text 节点 | 支持 | 保证范围 | 唯一源码定位、无脚本接管、上下文受支持 |
| UTF-8 BOM、LF/CRLF、中文与 Emoji | 支持 | 保证范围 | 必须保留未修改字节、BOM 和未修改行尾 |
| 同一个父元素内多个 Text 子节点 | 支持 | 单节点条件支持 | 显式选择具体片段，不对父元素整体设置 textContent |
| `<p>你好 <strong>世界</strong>！</p>` | 支持 | 分片编辑 | 每次只改一个文字片段；跨元素选区只读 |
| 空白折叠、实体字符 | 支持 | 条件支持 | 显示值与源码不一一对应，编码规则见 Patch 规范 |
| 本地 CSS、图片、字体 | 支持 | 只读 | 根目录外路径不自动扩权；不内嵌或重写资源 |
| 本地 JS、CSS 动画 | 条件支持 | 受脚本策略限制 | JS 交互预览与静态校稿分开；CSS 动画可保留 |
| JS 写入或框架管理的 DOM | 条件支持 | 只读 | 即使文字和源码碰巧相同，也不据此推断可编辑 |
| React/Vue 等编译后页面 | 条件支持 | 默认只读 | 不反推 JSX、模板、状态或 API 数据 |
| Canvas/WebGL 图表 | 条件支持 | 只读 | 图表上的文字不等于 HTML Text 节点 |
| SVG `<text>` / `<tspan>` | 条件支持 | 延后 | 涉及 XML 命名空间、实体、布局与 JS 生成 |
| iframe / srcdoc / Shadow DOM | 条件支持 | 只读 | 不跨 frame 或 shadow root 写回；远程 iframe 离线阻断 |
| CSS `::before/::after` 生成文字 | 支持 | 只读 | 文本来自样式，不存在可写的 HTML Text 节点 |
| input/textarea/select 的当前值 | 支持 | 只读 | value、默认值与用户输入状态不是普通文本节点 |
| 属性、alt/title/aria-label、HTML 注释、script/style 内容 | 支持或不可见 | 不编辑 | 可见说明文字可以属于普通段落；源码注释不在视觉编辑范围 |
| 缺失资源、外部 CDN、在线 API | 尽力 | 不保证效果 | 默认阻断网络，展示诊断，不承诺离线还原 |
| 非 UTF-8、无效 UTF-8、解析纠错导致的歧义 | 尽力只读 | 不覆盖 | 允许解释原因；首版不自动转码、不静默修复 |

HAE-003/004 已验证静态定位与纯字节候选生成；HAE-005 自动实验覆盖统一 Main 窗口中的草稿、另存、可信 IPC、文档/视图切换与关闭保护及 UI 崩溃后重连；HAE-009 现已接入产品编辑与保存。实际支持范围保持保守：解析错误（缺少 doctype 除外）和格式化元素克隆修复使整页只读；不连续/重叠文本两侧均拒绝；表单子树、template、特殊文本上下文、外来命名空间和 contenteditable 只读。声明式 Shadow DOM 或整树不一致拒绝整页映射；带生成文字的 `::before/::after/::marker` 祖先之下不开放选择，以避免覆盖式伪元素误认。候选重解析若改变标签结构、属性或其他文本，同样拒绝。详见 [映射反例](docs/implementation/HAE-003.md)、[Patch 验证](docs/implementation/HAE-004.md) 与 [集成实验](docs/implementation/HAE-005.md)。

HAE-008 第一阶段已验证 Main 的目录/入口授权、共享相对资源、入口切换输入保护和离线诊断 IPC；HAE-009 已提供产品诊断面板，原生对话框人工操作和产品模式切换仍待验收或实现，见 [目录阶段记录](docs/implementation/HAE-008.md)。

### 脚本执行与校稿模式：首版保守边界

交互预览可在隔离环境执行本地 JS，验证图表和交互。首版**保证保存的静态校稿视图禁用页面脚本**，仅允许验证过的源码文本；切换模式会重载，用户先处理草稿，界面提示动态部分可能消失或不同。

这样保留“真实 Chromium 预览”，但不承诺在任意 JS 持续运行时都能安全修改。HAE-003 验证的是禁用页面脚本的静态视图；交互模式没有映射。后续若要支持运行中的 JS 页面编辑，须新增 ADR、节点生命周期验证和回归语料。

产品默认打开静态校稿模式；当前本地脚本交互使用独立只读开发入口，产品模式切换尚未提供。应用默认无网络访问，V1 不提供一键放开任意网络的开关。运行未知页面代码仍有宿主运行时风险，隔离不是对任意恶意 HTML 的绝对安全承诺。

## 4. 明确排除

首版不做拖拽布局、增删元素、富文本格式刷、全局 CSS 重写、图片替换、图表数据编辑、PDF 内容或打印版式编辑、源代码 IDE、Git 客户端、批量跨文件替换、多人协同、云同步、插件市场和内置 AI 修改。Linux、Windows ARM 和 Intel Mac 不在首期发布矩阵内。

PDF 预览/导出是已明确新增的范围，不包含分页版式编辑或 PDF 回写。原讨论中的 CSS 变量、图片替换和 AI 辅助保留为后续独立提案，不与 MVP 绑定。没有可验证定位的编辑不能降级成整页导出覆盖。

## 5. 数据与保存约束

- 一次只编辑一个 HTML 入口。资源目录只读；切换入口不丢草稿。
- 初期上限候选为 HTML 5 MiB、100,000 个 DOM 节点、每批 1,000 个文本修改、单节点 64 KiB 新文本；这是待基准验证的保护值，不是性能结论。
- 校稿草稿与历史存在应用私有数据目录，不进入用户项目或页面可读资源域；可以清除最近记录和恢复数据。
- 保存 HTML 不执行脚本、不下载资源、不修改附属文件、不写入编辑器标记。
- 源文件从打开到保存之间被其他程序修改时，提供重新加载、另存草稿或取消；首版不自动合并。
- 目录项目的“另存为”只保存 HTML，默认定位在原目录，保持相对资源路径语义；跨目录保存提示资源不会随之复制。完整项目导出延后。

## 6. 非功能需求与成功指标

| ID | 目标 | 证据 |
| --- | --- | --- |
| NFR-01 | 每次保存，补丁范围之外字节完全一致 | 自动比较原始 Buffer 及未改资源哈希 |
| NFR-02 | 任何定位歧义、冲突、备份失败都不覆盖源文件 | 错误注入与磁盘内容断言 |
| NFR-03 | 已应用且已持久化的草稿可恢复 | 强制终止与重启测试，明确最后持久化点 |
| NFR-04 | 不可信预览无通用文件、Shell、Node 或高权限 IPC 能力 | 负向安全样例与跨会话攻击测试 |
| NFR-05 | 中文输入、键盘操作、缩放和屏幕阅读状态可用 | Windows 和 Mac 的人工交互记录 |
| NFR-06 | 打开与选择响应符合声明的性能预算 | 记录硬件/OS/Chromium/样例，报告 p50、p95 |

候选性能目标：1 MiB / 10,000 节点本地样例打开到可选择 p95 ≤ 3 秒，悬停高亮 ≤ 100 ms，100 个补丁校验与保存 ≤ 1 秒（不计异常磁盘重试）。HAE-013 实测后调整，不能用目标代替结果。

MVP 用户任务：在一份脱敏报告中修正标题、日期和 3 处表格文字，检查 Diff，保存，在独立浏览器重新打开；随后撤销一次、再次保存，并恢复一次备份。要求用户能够独立完成，原布局和未选源码不变。

覆盖率只报告固定语料的分子与分母，例如“20/20 个受支持样例完成编辑”。不宣称支持“90% 的 AI HTML”或“任意 HTML”。
